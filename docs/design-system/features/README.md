# Features

BDD feature files are canonical behavior definitions. Each scenario must include:

- one `@SCN-*` scenario id
- one category tag: `@happy_path`, `@safety_guardrail`, or `@failure_recovery`
