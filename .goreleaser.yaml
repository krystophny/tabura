version: 2
project_name: slopshell

builds:
  - id: slopshell
    main: ./cmd/slopshell
    binary: slopshell
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }} -X main.commit={{ .ShortCommit }}

archives:
  - id: slopshell
    ids:
      - slopshell
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "slopshell_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md
      - scripts/piper_tts_server.py
      - scripts/lib/llama.sh
      - scripts/setup-local-llm.sh
      - deploy/launchd/*.plist

checksum:
  name_template: checksums.txt

release:
  github:
    owner: sloppy-org
    name: slopshell
  replace_existing_artifacts: true
  extra_files:
    - glob: ./scripts/install.sh
      name_template: install.sh
    - glob: ./scripts/install.ps1
      name_template: install.ps1
  draft: false
  prerelease: auto

changelog:
  use: github-native

brews:
  - repository:
      owner: sloppy-org
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/sloppy-org/slopshell
    description: Local-first voice assistant with canvas UI
    license: MIT
    install: |
      bin.install "slopshell"
    caveats: |
      Run `slopshell server` or use the full installer:
        curl -fsSL https://github.com/sloppy-org/slopshell/releases/latest/download/install.sh | bash
      Requires codex app-server and Python 3.10+ for Piper TTS.
